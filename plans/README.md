# Implementation Plans

Generated by the improve skill on 2026-06-12 (audit of commit `224c6f5`,
2026-06-11). Sources: a full read of `C:\Jobs\Codex\CODEBASE_AUDIT_2026-06-11.md`
(independently re-verified against the code) plus three parallel audit passes
(security; performance + tech debt; tests + dependencies + DX), with every
finding vetted against the live code before planning.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, run its drift check first, honor its STOP
conditions, and update your row when done.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | EEO sexual-orientation answers use the right profile field | P1 | S | — | DONE (merged fc5b449; rule placed first in _EEO_RULES — "sex" substring shadowing) |
| 002 | Validate the final mutated resume file (DOCX/TEX) | P1 | M | — | DONE (merged 0970730; 250 tests pass) |
| 003 | pypdf dependency + one-page enforcement in the live pipeline | P1 | M | 002 | DONE (merged bfab8ca; 255 tests pass; pypdf installed in main venv) |
| 004 | Delete plaintext OAuth token on worker exit; sweep stale run dirs | P1 | M | — | DONE (merged ebe71c6; 146 TS tests pass) |
| 005 | Password handoff via 0600 file instead of child env | P2 | M | 004 | DONE (merged f515041; redaction preserved via secretsForRedaction; 146 TS + 265 PY pass) |
| 006 | Wire automation.autofillApprovedDefaults end to end | P2 | M | — | DONE (merged c890c26; default-off; EEO stays review-required; 146 TS + 269 PY pass) |
| 007 | Font detection counts style-inherited sizes | P2 | S | — | DONE (committed 636efd0; Normal-style sizes now histogrammed; explicit runs still dominate; 272 PY pass) |
| 008 | Cover-letter prompt/validator alignment; previously-employed aliases | P3 | S | — | DONE (committed e6cbf8d; banned list joined from validator; em dashes removed; 275 PY pass) |
| 009 | Event ingestion wrapped in one SQLite transaction | P2 | S | — | DONE (committed 68070ef; db.transaction wraps writes, broadcast stays outside; rollback test added; 147 TS pass) |
| 010 | Dependency hygiene: vendor Geist fonts, clear audit moderates, align better-sqlite3 | P2 | S | — | DONE-PARTIAL (committed dace0df; fonts vendored + both moderates cleared; Step 4 better-sqlite3 align REVERTED per STOP cond — collapsing to one v12 breaks rebuild-native.mjs's dual Node/Electron ABI strategy; residual out-of-scope esbuild high+low via tsx/vitest>vite remain so `audit --moderate` is not zero) |
| 011 | Path validation at the IPC contract boundary | P3 | S | — | DONE (committed f3b4822; AbsoluteLocalPathSchema on 3 request fields; 148 TS pass) |
| 012 | Unit tests for Gmail OAuth service + converter diagnostics | P2 | M | — | DONE (committed 039eb50; 5 converter + 10 oauth tests incl. loopback state-mismatch & happy path; only export keyword added to source; 163 TS pass) |
| 013 | Repo-root CLAUDE.md + README onboarding | P2 | M | — | DONE (committed b4847e1; CLAUDE.md 95 lines w/ 5 invariants + cmd table; README prerequisites + no-.env model. NOTE: verify's typecheck/test/test:python/python:audit all green; only `pnpm audit --audit-level high` fails on pre-existing esbuild advisory — documented in CLAUDE.md gotchas) |
| 014 | Lint/format toolchain (eslint, prettier, ruff) wired into verify | P3 | M | — | DONE (configs c-1; autofix c-2 f1e4cd5. lint:js + lint:python wired into verify, both exit 0; typecheck + 163 TS + 275 PY green. eqeqeq allows `== null`; 2 control-char regexes + 2 WIP run-console memos inline-disabled (TODO plan-017). DEFERRED per operator: prettier mass-reformat (~7000 mechanical lines on never-formatted code) NOT applied — prettier configured + format/format:check scripts exist but format:check is non-zero and intentionally NOT in verify; run a dedicated `pnpm format` pass later. solid/prefer-for downgraded to warn — 6 Array#map→<For> follow-ups) |
| 015 | Python requirements lock via pip-tools | P3 | M | 003 | DONE (committed 8b14730; requirements.in=intent, requirements.txt=138-pin lock incl. pypdf+pip-tools; fresh .venv-build rebuild → 275 PY pass, pip-audit clean; installers unchanged. Lock resolved under 3.12 = CI target (windows-latest + setup-python 3.12). NOTE: ensure-python-env.mjs uses bare `python.exe` host default — on a box where that's 3.14 the venv breaks (litellm<3.14); set APPLYO_PYTHON to a 3.12 interp. Pre-existing DX wrinkle, out of scope here.) |
| 016 | CI dependency caching + coverage reporting | P3 | S | — | DONE (committed 6264007; ci.yml: pnpm-action before setup-node + cache:pnpm, setup-python cache:pip keyed on requirements.txt lock, new Coverage step. @vitest/coverage-v8 + test:coverage (exit 0, 163 pass). vitest coverage scoped to source (out/ bundles excluded); dead 80% thresholds replaced with measured floors lines/stmts 37, fns 62, branches 63 (actuals 42.7/67.3/68.9 — renderer .tsx untested, ratchet w/ plan 017). coverage/ gitignored. DEFERRED: better-sqlite3 build cache (step 3, needs live CI). PENDING: 2-run cache-restore verification — no push this session; YAML validated locally) |
| 017 | Chat job cards go live (statuses from run events) | P2 | L | — | DONE-PARTIAL (committed; renderer-only chatLiveSync engine: 3s poll + per-run log triggers reconcile cards vs runs.list (source of truth, never persisted), new CARD_RUN_STATE action, JobCard full label coverage + "Open run" deep-link, getBatchProgress in-flight→running/SUBMITTED→completed. typecheck + lint + 177 TS tests pass (14 new). PENDING: manual `pnpm dev` observation of live progression (needs Electron + 3.12 venv, not run this session). NOTE: chat UI has NO css today (half-built) so new status-review/open-run classes are unstyled — left consistent with rest, styling deferred, not a done-criterion) |
| 018 | Consolidate the triplicated document export flow | P3 | M | 002, 003 | DONE-PARTIAL (committed; scoped to the 2 resume tails per operator after Step-1 STOP cond #2 — fallback/cover-letter/lazy-CL diverge structurally (no mutation event / no PDF stage / report-file threading) and stay inline. New documents/export_flow.run_resume_render_tail + ResumeRenderTailSpec; DOCX keeps one-page enforcement, TEX collapses to 1 call. Byte-for-byte events: 280 PY pass (5 new), ZERO replay-fixture edits, ruff clean, runner.py -94 lines. Plan's ≥250-line/grep=0 done-criteria intentionally NOT met (full consolidation rejected as not data-expressible). Enables plan 020.) |
| 019 | Split registerIpc.ts into domain handler modules | P3 | M | after 006, 011 | DONE-PARTIAL (committed; registerIpc.ts 707→33-line composition root + 12 `ipc/handlers/*.ts` modules, all <200 lines: context.ts (shared deps + path-security closures + handleContract; approvedPickedPaths stays module-private behind approvePickedPath/requirePickedPath), settings/provider/document/profile/file/jobQueue/run/runQuery/chat/system handlers + runStatus.ts shared helpers. Pure cut-paste, zero body edits. IPC surface grep identical before/after (46 channels, 46 handleContract calls, 1 ipcMain.on); typecheck + 177 TS tests + lint:js (0 errors) green. Deviations: (a) single cohesive commit, not per-domain slices — reviewable via --color-moved; (b) `pnpm verify:packaged` NOT run (needs Electron packaging + 3.12 venv, not available non-interactively — same deferral as plans 016/017); packaged smokes remain the authoritative net to run before release.) |
| 020 | Decompose runner.py (field resolution + document stage) | P3 | L | 018 | DONE (commits 22e4a20 + 33dc0f5; field_resolution.py 58 lines, document_stage.py 771 lines, runner.py 2739→1989. Both extractions pure moves with import/monkeypatch-target updates (test_runner_autofill_env, test_one_page_enforcement, test_final_artifact_validation, test_portal_replay_fixtures). 282 PY tests green at every step, ruff clean. NOTE: runner.py stops at 1989 lines, above the plan's ~1200 aspiration — the remainder is the portal/approval/control-loop core the plan explicitly excludes; per STOP conditions, stopped at what the two extractions achieve. Portal core extraction stays the next candidate after live-run soak.) |

## Dependency notes

- 003 after 002: both edit the same `runner.py` export region; 002 stabilizes it first.
- 005 after 004: 005 extends 004's worker-exit cleanup hook and janitor sweep.
- 015 after 003: the Python lock must include the new `pypdf` dependency.
- 018 after 002+003: their validation/page-count gates become parameters of the shared export flow.
- 019 after 006 and 011: both add code to `registerIpc.ts` / contracts; land features before the mechanical split.
- 020 after 018: export_flow extraction is the first module out of runner.py; 020 builds on that shape.
- 014's autofix commit will create merge noise for any plan in flight — avoid running 014 concurrently with anything else.
- 013 (CLAUDE.md) can run any time; if late, reflect whatever plans have landed.

## Finding → plan traceability

| Audit finding | Plan |
|---------------|------|
| CODEBASE_AUDIT P1 #4 (sexual orientation answer) | 001 |
| CODEBASE_AUDIT P1 #2 (final DOCX never validated) | 002 |
| CODEBASE_AUDIT P1 #1 + #3 (pypdf missing; one-page check dead) | 003 |
| SEC-01 upgraded + perf-agent SECURITY-01 (plaintext token + work-dir retention) | 004 |
| SEC-03 (password in child env) | 005 |
| CODEBASE_AUDIT P2 #6 (autofillApprovedDefaults never wired) | 006 |
| CODEBASE_AUDIT P2 #7 (style-defined font sizes) | 007 |
| CODEBASE_AUDIT P2 #9 + #10 (CL prompt drift; employed-by aliases) | 008 |
| PERF-02 reframed (event ingest atomicity) | 009 |
| DEP-006 (postcss moderate via geist→next), brace-expansion moderate, DEP-003 (better-sqlite3 split) | 010 |
| SEC-05 (contract-level path validation) | 011 |
| TEST-001 + TEST-002 (OAuth + converter diagnostics untested) | 012 |
| DX-003 + DX-002 (no CLAUDE.md; onboarding gaps; no-.env documentation) | 013 |
| DX-001 (no linter/formatter) | 014 |
| DEP-001 (no Python lock) | 015 |
| DX-004 + DX-005 (CI caching; dead coverage thresholds) | 016 |
| CODEBASE_AUDIT P2 #5 + PERF-03 (chat frozen at enqueue; queue polling) | 017 |
| DEBT-02 (triplicated export sequence) | 018 |
| DEBT-03 (registerIpc god function) | 019 |
| DEBT-01 (runner.py god module) | 020 |

## Findings considered and rejected

- **SEC-02 (OAuth loopback origin validation)**: state parameter is validated
  and the code is consumed immediately; origin headers add nothing against the
  realistic threat. Not worth doing.
- **SEC-04 (zeroing password strings in JS memory)**: not meaningfully
  achievable in a managed runtime; negligible practical gain. Not worth doing.
- **PERF-01 (list virtualization in Queue/History screens)**: self-refuting —
  the lists are hard-capped at 6 and 12 rows by `.slice()`; nothing to
  virtualize.
- **ARCH-01 (STORES.md documenting store boundaries)**: store layout is recent,
  small, and legible; a doc would go stale faster than it pays. CLAUDE.md
  (plan 013) covers the architecture rules instead.
- **ARCH-02 (cross-layer correlation IDs)**: `run_id` already threads through
  every WorkerEvent, DB row, and IPC broadcast — it IS the correlation ID.
- **TEST-003 (renderer screen logic tests, L)**: real but low-leverage as a
  standalone plan; the highest-value screen logic (chat) gets tested inside
  plan 017, and coverage reporting (016) will show where the next gap is.
- **TEST-004/TEST-005 (browser adapter / runner orchestration test layers, L)**:
  deferred — plan 020's decomposition is the prerequisite that makes these
  testable seams exist; revisit after it lands.
- **DEP-002/DEP-004/DEP-005 (vitest/electron/solid-router upgrade cadences)**:
  no action needed now; cadence note folded into plan 013's CLAUDE.md gotchas.
- **TEST-006/007/008**: positive findings (skip patterns sound, verify baseline
  strong, approval-gate smokes good) — nothing to do.
- **CODEBASE_AUDIT P2 #8 (wizard per-step persistence)**: contradicts the
  master plan's stated rule but the wizard is short and the loss window small;
  cost/benefit doesn't clear the bar while P1/P2 items remain. Revisit on user
  complaint.
