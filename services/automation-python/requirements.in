# Source of intent. Edit THIS file, then regenerate the lock:
#   python -m piptools compile requirements.in -o requirements.txt --strip-extras
litellm>=1.74.0
nodriver>=0.44
seleniumbase>=4.33.3
google-api-python-client>=2.170.0
google-auth-oauthlib>=1.2.1
pdf2docx>=0.5.8
python-docx>=1.1.2
TexSoup>=0.3.1
pytest>=8.3.5
pyinstaller>=6.15.0
tzdata>=2025.2
ruff>=0.8.0
pypdf>=6.15.0  # PYSEC-2026-3655, PYSEC-2026-3656

# dev tooling (kept in the lock so maintainers can regenerate it)
pip-tools>=7.6.1  # 7.5.3 cannot run under pip 26: it imports pip internals that were removed

# Transitive pins that only exist to clear advisories pip-audit blocks on.
# Each arrives through a parent that does not yet require the fixed release
# itself; the parent is named so the line can be dropped once it floats past
# this version on its own.
httplib2>=0.32.0  # via google-api-python-client. PYSEC-2026-3444
pyasn1>=0.6.4  # via google-auth. PYSEC-2026-3455, PYSEC-2026-3456, PYSEC-2026-3457
aiohttp>=3.14.3  # via litellm. PYSEC-2026-3545, PYSEC-2026-3546, PYSEC-2026-3547
cryptography>=50.0.0  # via google-auth. PYSEC-2026-3552

# tectonic>=0.15.0 is a binary tool, not a PyPI package — bundled via PyInstaller build script
